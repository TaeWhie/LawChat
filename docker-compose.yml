version: '3.8'

services:
  lawchat-api:
    build: .
    container_name: lawchat-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always
    volumes:
      - ./vector_store:/app/vector_store
      - ./api_data:/app/api_data
    environment:
      - PORT=8000
