# Streamlit Community Cloud 또는 로컬에서 사용할 Secrets 예시
# Cloud: 대시보드 Settings → Secrets에 아래 내용을 TOML로 붙여넣기
# 로컬: 이 파일을 secrets.toml 로 복사 후 값 채우기 (secrets.toml 은 .gitignore 대상)
# base_url = LawChat API 서버 URL (끝 / 제거)

[api]
base_url = "https://your-lawchat-api.onrender.com"
openai_api_key = "sk-..."
